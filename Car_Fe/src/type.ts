export type Car = {
    id?: number;                    // 고유 ID
    brand: string;                // 브랜드명 (예: Ford, Kia)
    model: string;                // 모델명 (예: Mustang, Rei)
    color: string;                // 색상
    registrationNumber: string;  // 차량 등록 번호
    modelYear: number;           // 출시 연도
    price: number;               // 가격
}